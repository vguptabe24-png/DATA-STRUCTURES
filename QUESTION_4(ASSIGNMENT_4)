#include <stdio.h>
#include <string.h>
#define SIZE 100

char queue[SIZE];
int front = 0, rear = -1;

int freq[256] = {0};  // frequency of characters

void enqueue(char ch) {
    queue[++rear] = ch;
}

void dequeue() {
    front++;
}

int isEmpty() {
    return front > rear;
}

int main() {
    char str[SIZE];
    printf("Enter the string: ");
    fgets(str, SIZE, stdin);  // input with spaces too

    int n = strlen(str);
    if (str[n-1] == '\n') str[n-1] = '\0'; // remove newline

    printf("Output: ");
    for (int i = 0; str[i] != '\0'; i++) {
        char ch = str[i];

        if (ch == ' ') continue; // ignore spaces

        freq[ch]++;
        enqueue(ch);

        // remove characters from queue front if they are repeated
        while (!isEmpty() && freq[queue[front]] > 1) {
            dequeue();
        }

        if (isEmpty())
            printf("-1 ");
        else
            printf("%c ", queue[front]);
    }
    printf("\n");
    return 0;
}
